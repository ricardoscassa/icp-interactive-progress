{
  "name": "icp-drawing-recognition-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone TypeScript prototype for construction drawing upload, room recognition, boundary suggestions and manual polygon editing.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "node scripts/test.mjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
